const STORAGE_KEYS = {
  // 登录token(pin)
  AUTH_TOKEN: 'auth_token',
  // openId
  OPEN_ID: 'open_id',
  // 第三方平台自定义配置文件（积理studio）
  EXT_CONFIG: 'ext_config',
  // 全链路日志跟踪ID
  TRACEID: 'traceId',
}

module.exports = {
  STORAGE_KEYS,
}
